# Local development environment
APP_ENV=development

# Replace this locally. Never commit a real secret key.
SECRET_KEY=replace-with-a-random-secret-key

# Local SQLite database
DATABASE_URL=sqlite:///expense_tracker.db
